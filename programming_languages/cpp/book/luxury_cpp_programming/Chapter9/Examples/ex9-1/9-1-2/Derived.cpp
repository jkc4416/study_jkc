// Derived.cpp
#include "Derived.h" // Derived 클래스의 선언을 포함하기 위해 Derived.h 파일을 포함

// Derived 클래스의 멤버 함수 정의
void Derived::f() {
    cout << "Derived::f() called" << endl; // "Derived::f() called" 문자열을 출력
}
